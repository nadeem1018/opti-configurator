import { BlankhandlerPipe } from './blankhandler.pipe';

describe('BlankhandlerPipe', () => {
  it('create an instance', () => {
    const pipe = new BlankhandlerPipe();
    expect(pipe).toBeTruthy();
  });
});
